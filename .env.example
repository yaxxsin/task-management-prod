# ================== AI API CONFIGURATION ==================
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Ollama Connection (configured via Settings in-app by default)
# Default host is http://localhost:11434

# ================== BACKEND API URL ==================
# Use your Cloudflare Tunnel URL or local IP for production
# Example: VITE_API_URL=http://192.168.1.100:9008
VITE_API_URL=http://localhost:9008

# ================== SERVER CONFIGURATION ==================
# Port for the backend server (optional, defaults to 9008)
PORT=9008

# JWT Secret - REQUIRED! Use a strong random string (min 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_STRING_MIN_32_CHARS

# Frontend URL - Used for password reset links
# Change this to your Cloudflare Tunnel URL in production
FRONTEND_URL=http://localhost:5173

# ================== CORS CONFIGURATION ==================
# Comma-separated list of allowed origins
# Leave empty to allow all origins (dev mode only!)
# Example: ALLOWED_ORIGINS=https://your-tunnel.trycloudflare.com,http://localhost:5173
# ALLOWED_ORIGINS=
